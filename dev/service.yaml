apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: myapp
spec:
  selector:
    app: myapp
  ports:
    - name: metrics      # <--- port name
      port: 443          # <--- service port
      targetPort: 443    # <--- pod container port
